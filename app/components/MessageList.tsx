// ---------------------------------------------------------------------------
// UI: renders the list of chat messages.
//
// Responsibility: pure presentation. Takes messages as props and renders
// user vs assistant bubbles. Good place to also render the "sources" /
// citations returned alongside an answer so you can see WHICH chunks the
// model used (very useful for debugging retrieval quality).
//
// TODO: build the presentational component (props in, JSX out, no fetching).
// ---------------------------------------------------------------------------
