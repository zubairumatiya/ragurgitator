// Prefetchable shell for this force-dynamic page — see AppraiseLoading for why
// the boundary sits in each leaf segment rather than at app/appraise/.
export { AppraiseLoading as default } from "@/app/components/AppraiseLoading";
