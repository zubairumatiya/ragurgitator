// Placeholder until Phase 2: the pure half of the semantic cache gate.
export async function run(command: "score" | "baseline" | "gate", _args: string[]): Promise<void> {
  throw new Error(`${command} is not built yet (docs/semantic-cache-gate-plan.md Phase 2)`);
}
